# S##. {スライス名（日本語）}— テスト結果

**成熟度: `L1 動く`** ｜ 要求: [S##](../usdm/src/S##-{name}.html) ｜
設計: [S##](../design/S##-{name}.md) ｜ ハブ: [S##](../slices/S##-{name}.md)

> **実測しか書かない**（絶対ルール 7）。件数・出力は実行して得たものだけ。
> 落ちているなら落ちていると書く。

## 実行環境

- 実行日: {YYYY-MM-DD}
- 全体: `{プロファイルの「テスト（全体）」コマンド}`
- 統合テスト: `{プロファイルの「統合テスト（E2E）」コマンド}`
- 件数の数え方: `{プロファイルの「テスト件数の数え方」コマンド}`

## 結果の要約

```
$ {実際に実行したコマンド}
{実際の出力（末尾の集計行を含む）}
```

| 範囲 | passed | failed | skipped |
|---|---|---|---|
| 全体 | {n} | {n} | {n} |
| このスライス分 | {n} | {n} | {n} |

- 骨組みの E2E: {緑 / 赤（赤なら回帰なので直してから完了）}

## 仕様とテストの対応

> 仕様番号は要求（USDM）の番号をそのまま使う。
> **未カバーの仕様も行を残す** —— 消すと「無かったこと」になる。

| 仕様 | 種別 | テスト | 結果 |
|---|---|---|---|
| {番号}-1 | 単体 | `{テストファイル}::{テスト名}` | 緑 |
| {番号}-1 | 統合 | `{テストファイル}::{テスト名}` | 緑 |
| {番号}-2 | —— | （未カバー） | L2 で足す |

## 実物の出力

> テストの緑だけでは完走の証拠にならない。実際に動かした出力を貼る。

```
$ {プロファイルの「動作確認」コマンド}
{実際の出力}
```

## 気づいたこと

- {不安定なテスト・遅いテスト・skip の理由。無ければ「なし」}
