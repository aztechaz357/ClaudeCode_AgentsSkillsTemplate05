# S##. {スライス名（日本語）}— 要求仕様（厚い経路）

**📝 要求のみ・未設計**。依存: [S##](S##-<name>.md)（{依存理由}。無ければ「依存なし」）。

## 背景と課題

- {なぜ欲しいのか。今できなくて何に困っているか}
- 出典: {ユーザー要望の言葉 / バックログの行 / 不具合報告}
- 厚い経路を使う理由: {不可逆 / 外部公開 / 安全機構 / データ形式 / 3 回失敗}

## ユーザー価値

- {できるようになると何が変わるか。体験で書く（実装手段では書かない）}

## 受け入れ条件

> 統合テストのテストケースにそのまま翻訳される。
> 「入力 → 期待される観測結果」の形で、境界値と失敗経路を必ず含める。

- [ ] {条件1: 入力・操作 → 期待される観測結果（数値・境界値を明示）}
- [ ] {条件2: 失敗経路 → 期待される振る舞い}
- [ ] {条件3: 既存機能の回帰がないこと（具体的に何が壊れないか）}

## スコープ / 対象外

- **入れる**: {今回の範囲}
- **入れない**: {意図的に外す範囲と理由（設計書の「既知の制限」と整合させる）}

## 既存機能との関係

- {バックログの他スライス・実装済み機能との重複・競合・依存}

## 制約

- {技術制約（プロファイルの依存方針・対応環境・性能要件）}
- {運用制約（既存データとの互換・移行の要否）}

## スライス分割の当たり（大きい要望の場合のみ）

| 順 | スライス案 | 内容 | 厚い経路が必要か |
|---|---|---|---|
| 1 | S##（候補） | {入口から出口まで通る単位} | {必要 / 不要（薄い 1 枚へ）} |

## 未確定事項

- {ユーザーに確認すべきこと（AskUserQuestion で聞く事項）。
  確認が済んだら回答を追記して確定させる}
