# タスクリスト: {S## スライス名}

**目標成熟度: {現在 L{n} → 目標 L{n+1}}** ｜
進捗の正: [docs/backlog.md](../../docs/backlog.md)

## 正の文書（ここに書き写さない。リンク先を読む）

- 要求仕様書: [S##](../../docs/usdm/src/S##-<name>.html) —— 要求・理由・仕様・トレースの正
- 設計書: [S##](../../docs/design/S##-<name>.md) —— 層・契約・判断の記録の正
- ハブ: [S##](../../docs/slices/S##-<name>.md) —— 7 点への索引・実績・手抜き

厚い経路（`/add-feature` のときだけ追加で存在する）:

- 要求仕様書（厚い）: [S##](../../docs/requirements/S##-<name>.md)
- 実装前設計書（厚い）: [S##](../../docs/design/proposals/S##-<name>.md)

> **正を抜粋してこのファイルに書き写さない** 。抜粋は必ずドリフトし、
> 抜け落ちた条件のない仕様に対して実装する事故になる。
> 実装中の判断は `decisions.md` へ、恒久的に残す判断は
> スライス文書（厚い経路なら設計書）の側へ書く。
>
> 完了条件は `.claude/skills/agile-process/maturity.md` の
> **目標レベルの列だけ** 。上のレベルの条件で自分を止めない。

## 🚨 原則

- **目標成熟度の範囲内で作業する** 。L1 の反復で失敗経路を網羅する・
  契約を増やす・文書を作り込むのは越権（次の反復の仕事）
- 全タスクが `[x]` になるまで作業を継続する。「時間の都合」でのスキップは禁止
- スキップは技術的理由（方針変更で不要化）のみ許可し、
  `- [x] ~~タスク名~~（不要: 具体的な技術的理由）` の形式で記録する
- 1 タスク = 1 マイクロコミット（テスト先行 → Red → 実装 → Green → コミット）
- **半日で終わらなければスライスが大きすぎる** 。分割して報告する

## ⚠️ 前の反復からの申し送り

前回の振り返りから、今回気をつけることを転記する。無ければ「なし」。

- {例: E2E が内部構造に依存していて、リファクタで壊れた}

---

## 7 点セット（この 8 段を順に消化する。行は増やさない）

> 順序は動かせない。1 つでも次の反復に送ったら、その反復は未完了。
> 段が終わるたびに `INDEX.md` へ 1 行足し、`docs/backlog.md` の
> 「いま着手中」を `S## 段n {何を}（担当）` に更新する。

- [ ] **段1 要求仕様書**（`requirement-writer`）
      `docs/usdm/src/S##-<name>.html`。要求 1・理由 1 行・仕様 3 条。
      → `build_usdm.py` が 0 ／ **節目報告①**
- [ ] **段2 設計書**（`designer`）
      `docs/design/S##-<name>.md`。図を 1 枚 ＋ 構成 10 行 ＋ 判断の記録。
      → `check_diagrams.ps1` が 0 ／ **節目報告②（却下案を 1 行で）**
- [ ] **段3 単体テスト**（`unit-tester`）
      対象: `{テストルート}/...`。テスト先行で書き **Red を確認**（件数を記録）
- [ ] **段4 実装**（`coder`）
      対象: `{ソースルート}/...`。Red を消す最小限。差分 20〜30 行で区切る
- [ ] **段5 統合テスト**（`integration-tester`）
      対象: `{統合テストルート}/...`
- [ ] **段6 テスト結果まとめ**（`test-summarizer`）
      `docs/test-reports/S##-<name>.md`。実測の件数・仕様対応表・実物の出力
- [ ] **段7 マニュアル**（`manual-writer`）
      `docs/manual.md` の共通 3 節を維持し `## S##` 節を追加（実測の出力）
- [ ] **段8 検証と記録**（`implementation-validator` → `doc-syncer`）
      → **節目報告③（5 行サマリ）**

## 段5 の中身（統合テストは飛ばさない）

- [ ] **既存の E2E が緑**（骨組み由来を含む。赤ければ回帰なので先に直す）
- [ ] 仕様に対応する E2E を追加
      （L1 は正常系 1 本。L2 は 1 条につき 1 本以上）
- [ ] プロファイルの動作確認コマンドで **実物を動かし出力を得た**

## 完了の機械確認（目視で代用しない）

- [ ] 全テストが緑（passed / skipped 数を振り返りに記録）
- [ ] implementation-validator の検証をパス
      （プロンプトに **目標成熟度** を明記して起動する）
- [ ] `<ツール実行コマンド> .claude/tools/build_usdm.py` が 0
      （理由の欠落・番号の不整合・ **トレースの切れ** が無い）
- [ ] `<ツール実行コマンド> .claude/tools/check_deliverables.py --slice S##` が 0
- [ ] `<ツール実行コマンド> .claude/tools/build_structure.py --check` が 0
      （構成を変えた反復のみ。STALE なら再生成する）
- [ ] Red を確保できなかったタスクの変異テストで KILLED を確認
      （`<ツール実行コマンド> .claude/tools/mutate.py ...`）
- [ ] （L2 以降）外部 I/O が契約経由かをアーキテクチャテストで確認
- [ ] （設計の型に core 無変更があり、対象が `core_files.txt` に載っている場合のみ）
      `check_unchanged.py --since <開始コミット>`

## 記録

- [ ] ハブの「成熟度」「実績（実際の出力）」「残した手抜き」を更新
- [ ] 要求 HTML の検証欄を `☑` にし、 **トレース表の 5 列すべて** を埋めた
- [ ] `docs/backlog.md` の成熟度・次の一手・いま着手中を更新
- [ ] 残した手抜きを負債表へ転記（`D##` 採番・痛み・返す条件）
- [ ] （L3 のときだけ）`docs/design.md` へ統合し、リファレンスを同期
- [ ] 振り返り（下部に記録）

---

## 振り返り

### 完了日 / かかった時間

{YYYY-MM-DD} / {半日以内 or 超えた（理由）}

### 到達レベル

{L{n} → L{n+1}}（目標どおり / 未達（理由））

### 計画と実績の差分

- **計画と異なった点**: {想定していなかった変更点と理由}
- **追加したタスク**: {実装中に追加したタスクと理由}
- **技術的理由でスキップ**: {タスク名}: {理由} / 代替: {何に置き換わったか}

### テスト結果

- ユニット: {passed / skipped}
- E2E: {passed / skipped}

### 残した手抜き（負債表へ転記済みか）

| 内容 | 痛み | 返す条件 | 負債 |
|---|---|---|---|
| {内容} | {高/中/低} | {条件} | D## |

### プロセス改善提案

`reports/` の知覚・行動ログを読み返して記入する。

| 観測した事象 | 分類 | 改善案（対象ファイル） |
|---|---|---|
| {同じ手作業を N 回繰り返した} | ツール化 | {`.claude/tools/<name>` の新設} |
| {エージェントが判断に迷った} | 定義の曖昧さ | {`.claude/agents/<name>.md` の具体化} |
| {LLM に考えさせすぎた箇所} | 決定論化 | {チェックリスト・機械検証への置き換え} |
| {プロファイルに無くて推測した項目} | プロファイル不足 | {`CLAUDE.md` への追記} |
| {目標より上のレベルの作業をした} | **成熟度の指示漏れ** | {起動プロンプトへの明記} |
| {成果物を 1 つ落としかけた} | **段の飛ばし** | {該当エージェント定義の完了条件を厳しくする} |
| {半日で終わらなかった} | **切り方の失敗** | {スライス分割の基準を見直す} |

下の 3 行はこのプロセス特有の失敗。該当したら優先して記録する。

### 段ごとの所要（どこで詰まるか）

| 段 | 1 要求 | 2 設計 | 3 単体 | 4 実装 | 5 統合 | 6 まとめ | 7 マニュアル | 8 記録 |
|---|---|---|---|---|---|---|---|---|
| 分 | {n} | {n} | {n} | {n} | {n} | {n} | {n} | {n} |

### 次の反復への申し送り

- {次で気をつけること}
