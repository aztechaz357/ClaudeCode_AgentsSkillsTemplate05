# S##. {スライス名（日本語）}

**成熟度: `L0 未着手`** ｜ 依存: {`S##` または「依存なし」} ｜
バックログ: [docs/backlog.md](../backlog.md)

> 成熟度の完了条件は `.claude/skills/agile-process/maturity.md` を正とする。
> 進捗はバックログが正。この文書には進捗を書かない（成熟度の 1 行だけ）。

## 要求

> 記法の正は `.claude/skills/usdm/SKILL.md` 。
> **L1 では要求 1 個・理由 1 行・仕様 1〜3 条** 。超えるならスライスが
> 大きすぎる（分割する）。番号は S## と一致させる（`S02` なら `REQ2`）。
> 書いたら `<ツール実行コマンド> .claude/tools/build_usdm.py` で検証する。

### 【REQ{番号}】{〜したい}

- **理由**: {なぜ困っているか。 **要求の外側にある事実** を書く。
  「必要だから」「〜できないと不便」は理由になっていない}
- **範囲**: {要求の及ぶ境界。対象外を明示する。無ければこの行を消す}

**仕様**:

- [ ] `<{番号}-1>` {入力・操作 → 観測できる出力（数値・境界値を明示）}

<!--
L2 に上げる反復で、同じ要求の「仕様」に追記する:

- [ ] `<{番号}-2>` {境界値: 空・最大・0 件のとき何が出るか}
- [ ] `<{番号}-3>` {失敗経路: 前提が欠けたとき何が出るか（終了コード・メッセージ）}

要求を分割する必要が出たら 【REQ{番号}.1】 を足す（それぞれに理由が要る）。
検証済みになった仕様は `[x]` にする（対応するテストが緑になったとき）。
-->


## 薄い設計（5 行）

- 触るファイル: {`パス`（新規/追加）}
- 入口: {`関数・メソッド名(引数)` → `次に呼ぶもの`}
- 流れ: {入力 → … → 出力}
- 層: {例: presentation → application。逆流の有無}
- 仮実装: {L1 で手を抜く箇所。無ければ「なし」}

## 実績（実際に動かした記録）

> **推測で書かない**（絶対ルール 7）。実行して得た出力をそのまま貼る。

```
$ {実行したコマンド}
{実際の出力}
```

- E2E テスト: {`テストファイルのパス`}（{件数} 件・緑）
- 到達日: {YYYY-MM-DD} ／ コミット: {ハッシュ}

## 残した手抜き

> 記録された手抜きだけが許される。ここに書いた行を `docs/backlog.md` の
> 負債表へ転記する（`D##` を採番）。返したら `済` を付ける（行は消さない）。

| 内容 | 痛み | 返す条件 | 負債 | 状態 |
|---|---|---|---|---|
| {例: 出力先を標準出力にハードコード} | 低 | {ファイル出力の要求が出たとき} | D01 | 未 |

## 決めたこと（あとで思い出せるように）

- {判断と理由。1 行ずつ。無ければ「なし」}
