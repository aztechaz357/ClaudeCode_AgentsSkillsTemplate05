# S##. {スライス名（日本語）}

**成熟度: `L0 未着手`** ｜ 依存: {`S##` または「依存なし」} ｜
バックログ: [docs/backlog.md](../backlog.md)

> 成熟度の完了条件は `.claude/skills/agile-process/maturity.md` を正とする。
> 進捗はバックログが正。この文書には進捗を書かない（成熟度の 1 行だけ）。

## 要求

> 要求の正は下の HTML 1 枚。 **ここに要求の本文を書き写さない**
> （二重管理になり、必ず食い違う）。記法の正は
> `.claude/skills/usdm/SKILL.md` 。

- 要求（USDM）: [docs/usdm/src/S{番号}-{name}.html](../usdm/src/S{番号}-{name}.html)
- 要求一覧（生成物）: [docs/usdm/index.html](../usdm/index.html)

<!--
1. `.claude/skills/usdm/template.html` を
   `docs/usdm/src/S{番号}-{name}.html` にコピーする
2. `{}` を埋める。 **L1 では要求 1 個・理由 1 行・仕様 1〜3 条** 。
   超えるならスライスが大きすぎる（分割する）。
   要求ID は S## と一致させる（`S02` なら `REQ2`）
3. `<ツール実行コマンド> .claude/tools/build_usdm.py` で検証と再生成
4. 上のリンクのパスを実物に合わせる
-->


## 薄い設計（5 行）

- 触るファイル: {`パス`（新規/追加）}
- 入口: {`関数・メソッド名(引数)` → `次に呼ぶもの`}
- 流れ: {入力 → … → 出力}
- 層: {例: presentation → application。逆流の有無}
- 仮実装: {L1 で手を抜く箇所。無ければ「なし」}

## 実績（実際に動かした記録）

> **推測で書かない**（絶対ルール 7）。実行して得た出力をそのまま貼る。

```
$ {実行したコマンド}
{実際の出力}
```

- E2E テスト: {`テストファイルのパス`}（{件数} 件・緑）
- 到達日: {YYYY-MM-DD} ／ コミット: {ハッシュ}

## 残した手抜き

> 記録された手抜きだけが許される。ここに書いた行を `docs/backlog.md` の
> 負債表へ転記する（`D##` を採番）。返したら `済` を付ける（行は消さない）。

| 内容 | 痛み | 返す条件 | 負債 | 状態 |
|---|---|---|---|---|
| {例: 出力先を標準出力にハードコード} | 低 | {ファイル出力の要求が出たとき} | D01 | 未 |

## 決めたこと（あとで思い出せるように）

- {判断と理由。1 行ずつ。無ければ「なし」}
