# S##. {スライス名（日本語）}

**成熟度: `L0 未着手`** ｜ 依存: {`S##` または「依存なし」} ｜
バックログ: [docs/backlog.md](../backlog.md)

> この 1 枚は **ハブ** 。7 点セットへのリンクと、実績・残した手抜き・
> 決めたことだけを持つ。 **各成果物の本文をここへ書き写さない**
> （二重管理になり、必ず食い違う）。
> 成熟度の完了条件は `.claude/skills/agile-process/maturity.md` を正とする。
> 進捗はバックログが正。この文書には進捗を書かない（成熟度の 1 行だけ）。

<!--
チケット追跡が `local` のときは、この位置（H1 の直後）に `## チケット` 節が
入る。 **手で書かない** —— `sync_issues.py --apply` が起票し、状態・目標・
履歴を保つ（規約: .claude/skills/issue-tracking/SKILL.md）。
`github` / `gitlab` のときは Issue 側にあるので、この節は作らない。
既にある履歴は、モードを切り替えても消さない。
-->

## 成果物（7 点セット）

| # | 成果物 | 場所 | 状態 |
|---|---|---|---|
| 1 | 要求仕様書 | [S##](../usdm/src/S##-{name}.html) | {未 / 済} |
| 2 | 設計書 | [S##](../design/S##-{name}.md) | {未 / 済} |
| 3 | 単体テスト | `{テストルート}/{層}/test_{name}.py` | {未 / 済} |
| 4 | 実装 | `{ソースルート}/{層}/{file}` | {未 / 済} |
| 5 | 統合テスト | `{統合テストルート}/test_{name}.py` | {未 / 済} |
| 6 | テスト結果まとめ | [S##](../test-reports/S##-{name}.md) | {未 / 済} |
| 7 | マニュアル | [docs/manual.md](../manual.md) の `## S##` 節 | {未 / 済} |

- 仕様 1 条ごとの線は、要求 HTML の **トレース表** が正
  （`build_usdm.py` が欠けを落とす）
- 一式がそろっているかは機械で確認する:
  `<ツール実行コマンド> .claude/tools/check_deliverables.py --slice S##`

## 実績（実際に動かした記録）

> **推測で書かない**（絶対ルール 7）。実行して得た出力をそのまま貼る。
> 詳しい件数と仕様の対応はテスト結果まとめが持つ。ここには要約だけ。

```
$ {実行したコマンド}
{実際の出力}
```

- テスト: {全体 n 件緑 / E2E n 件緑}（詳細: [テスト結果](../test-reports/S##-{name}.md)）
- 到達日: {YYYY-MM-DD} ／ コミット: {ハッシュ}

## 残した手抜き

> 記録された手抜きだけが許される。ここに書いた行を `docs/backlog.md` の
> 負債表へ転記する（`D##` を採番）。返したら `済` を付ける（行は消さない）。

| 内容 | 痛み | 返す条件 | 負債 | 状態 |
|---|---|---|---|---|
| {例: 出力先を標準出力にハードコード} | 低 | {ファイル出力の要求が出たとき} | D01 | 未 |

## 決めたこと（あとで思い出せるように）

> **設計の判断は設計書の「判断の記録」が正** 。ここには、設計書に
> 載らない進め方の判断（分割・順序・打ち切り）だけを書く。

- {判断と理由。1 行ずつ。無ければ「なし」}
