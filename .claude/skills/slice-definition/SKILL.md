---
name: slice-definition
description: 縦切りスライス 1 本を、要求（docs/usdm/src/S##-<name>.html）とスライス文書（docs/slices/S##-<name>.md）の 2 枚で定義するスキル。薄い設計・実績・残した手抜きをスライス文書が持つ。要求仕様書と設計書を分けずに書く既定の経路。反復に着手する前、スライスの成熟度を上げる前に読み込む。
allowed-tools: Read, Write, Edit, AskUserQuestion
---

# スライス定義スキル（slice-definition）

反復開発の既定の文書は **スライス 1 本につき 2 枚** 。
要求仕様書と設計書は分けず、成熟度に応じて 2 枚が育っていく。

```
docs/usdm/src/S##-<name>.html   … 要求（USDM の表）。要求の正
docs/slices/S##-<name>.md       … 薄い設計・実績・残した手抜き
```

要求だけ別ファイルなのは、要求が **表として読まれる** ものだからで、
役割が違うわけではない。スライス文書の「要求」節にはリンクだけを置き、
要求の本文を書き写さない（二重管理になる）。

厚く書く必要があるスライス（不可逆・外部公開・安全機構・データ形式）だけ、
`requirements-definition` → `functional-design` の重い経路を使う。
判定条件は `agile-process` スキルの「いつ厚く書くか」を正とする。

## 要求は USDM で書く

要求（要求・理由・仕様）の **記法の正は `usdm` スキル** 。
ここでは重複させない。要点だけ:

- `.claude/skills/usdm/template.html` を `docs/usdm/src/S##-<name>.html`
  にコピーして埋める。要求ID は `REQ<番号>` で、番号は S## と一致させる
- **理由は必須** 。無いと `build_usdm.py` が終了コード 1 で落とす
- 仕様は `<tr class="spec">` の行。検証欄 `□` / `☑` が検証状態

## 分量の上限（守ること）

L1 に着手する時点で書くのは、次の 3 つだけ。

| 節 | 上限 | 内容 |
|---|---|---|
| 要求と理由 | 要求 **1 個** ・理由 **1 行** | 「〜したい」＋なぜ困っているか |
| 仕様 | **3 条** | 正常系のみ。「入力 → 観測できる出力」 |
| 薄い設計 | **5 行** | 触るファイル・入口の関数名・データの流れ |

**上限を超えたら、書きすぎではなくスライスが大きすぎる。** 分割する。

L2 に上げるときに仕様へ境界値・失敗経路を追加し、
L3 に上げるときに `docs/design.md`（現状設計 1 枚）へ内容を移す。

## 要求・理由・仕様の書き方

L1 では **要求 1 個・理由 1 行・仕様 1〜3 条** だけ。
境界値と失敗経路は L2 の仕事なので、ここで考え込まない
（考え込むのが重さの正体）。

**L1 で書くもの**（`docs/usdm/src/S01-count.html` の `<tbody>`）:

```html
<tr class="requirement">
  <td class="category">集計</td><td class="kind">要求</td>
  <td class="id">REQ1</td><td class="body">CSV の行数をすぐ知りたい</td>
</tr>
<tr class="reason">
  <td></td><td></td><td class="kind">理由</td>
  <td class="body">受領した CSV が想定件数かを Excel で開いて確認しており、
    10 万行だと開くだけで数分かかる</td>
</tr>
<tr class="spec">
  <td></td><td class="check">□</td><td class="id">1-1</td>
  <td class="body">tool count data.csv を実行するとデータ行数が「N 行」の形で
    標準出力に出る</td>
</tr>
```

**L2 で足すもの**（このスライスを L2 に上げる反復で、同じ要求に追記する）:

```html
<tr class="spec">
  <td></td><td class="check">□</td><td class="id">1-2</td>
  <td class="body">空ファイルを渡すと「0 行」が出る（ヘッダのみも 0 行）</td>
</tr>
<tr class="spec">
  <td></td><td class="check">□</td><td class="id">1-3</td>
  <td class="body">存在しないパスを渡すと終了コード 1 とパスを含むエラーが出る</td>
</tr>
```

チェックポイント（L1 用。4 つだけ）:

- [ ] 要求が「〜したい」の形か（「〜機能」「〜すること」になっていないか）
- [ ] **理由が要求の外側にある事実か**（要求の言い換えになっていないか）
- [ ] 仕様が「入力 → 観測できる出力」の形で、E2E テストに翻訳できるか
- [ ] クラス名・ライブラリ名を書いていないか（それは薄い設計の領分）

## 薄い設計の書き方

図もクラス図もシーケンス図も **書かない** （L3 で必要になったら
`docs/design.md` に書く）。5 行で足りる。

```markdown
- 触るファイル: `src/tool/cli.py`（追加）、`src/tool/count.py`（新規）
- 入口: `cli.main(argv)` → `count.count_rows(path) -> int`
- 流れ: 引数 → ファイル読み → 行数 → 標準出力
- 層: presentation（cli）→ application（count）。逆流なし
- 仮実装: ヘッダ判定は「1 行目を常にヘッダとみなす」で済ませる
```

「仮実装」の行が書けているかが重要。 **ここに書いた仮実装が、
そのまま負債表の 1 行になる** 。

## 進め方

1. **知覚**: `docs/backlog.md` の対象行・`docs/concept.md` のゴール・
   隣接する既存スライス文書を読む。既に動いているコードがあれば実物を見る
2. **書く**: `.claude/skills/usdm/template.html` を
   `docs/usdm/src/S##-<name>.html` にコピーして要求・理由・仕様を埋め、
   `./template.md` を写してスライス文書（薄い設計とリンク）を作る
3. **確認**: 曖昧なままでも **L1 なら進んでよい** 。ただし次の 3 つだけは
   AskUserQuestion で確認する（後戻りが大きいため）:
   - **理由**（推測で書かない。理由の推測は仕様の推測より害が大きい）
   - 出力の形式（他のものがこれを読む場合）
   - 不可逆な操作を含むかどうか
4. **検証**: `<ツール実行コマンド> .claude/tools/build_usdm.py` を実行し、
   USDM 違反が 0 件であることを確認する（理由の欠落・番号の不整合を検出する）
5. **反復後**: 検証済みになった仕様の `□` を `☑` に変え、「実績」に実際の
   出力を貼り、「残した手抜き」を埋め、バックログの負債表へ転記して、
   要求一覧を再生成する

## 成熟度を上げるときの追記

| 上げる先 | 追記する節 | やること |
|---|---|---|
| L2 | 仕様 | 境界値・失敗経路・既定値を追加し、テストを 1 条 1 本以上に |
| L2 | 残した手抜き | 仮実装を消し、消した行に `済` を付ける |
| L3 | （移す） | 構造の内容を `docs/design.md` へ移し、ここはリンクにする |

## 品質チェック

- [ ] **`build_usdm.py` が 0 件で通る**（理由の欠落・番号の不整合がない）
- [ ] 要求が「〜したい」で、理由が要求の外側にある事実か
- [ ] 仕様が 3 条以内（L1 の場合）
- [ ] 薄い設計が 5 行以内で、触るファイルと入口の関数名が書かれている
- [ ] 仮実装・ハードコードが「残した手抜き」に列挙されている
- [ ] 実績に **実際に実行した出力** が貼られている（推測禁止・絶対ルール 7）
- [ ] `docs/backlog.md` の成熟度と負債表が同期している
