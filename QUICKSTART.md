# クイックスタート

**覚えるのは 3 つだけ。**

| これがやりたい | これを打つ |
|---|---|
| 小さい道具が 1 本欲しい | `/tool <1 行で説明>` |
| 腰を据えて何か作りたい | `/backlog init` → `/skeleton` → `/iterate` |
| 次に何をやればいいか分からない | `/backlog 次` |

以下は、その 3 つを実際の場面に当てはめた台本です。

## 0. 導入（最初の 5 分・1 回だけ）

> **このリポジトリの中で以下の手順を実行しても動きません。**
> ここは配布元のテンプレートであり、`CLAUDE.md` はプレースホルダー（`{}`）の
> ままです。 **コピー先で `/setup-project` を走らせて初めて実地で通ります。**

```
1. .claude/ と CLAUDE.md を対象プロジェクトへコピーする
2. コピー先のディレクトリで Claude Code を開く
3. /setup-project
```

コピーするのは `.claude/` と `CLAUDE.md` の 2 つだけです
（`workshop/` はこのリポジトリ自身の作例なので持っていきません）。

`/setup-project` は言語・テストコマンド・ディレクトリ構成を対話で聞き、
`CLAUDE.md` の「プロジェクトプロファイル」を埋めます。
**これを飛ばして開発レーンを始めない** —— エージェントはプロファイルを
正として動くので、空欄があるとコマンドやパスを推測し始めます。

**始める前の確認は 1 つだけ** —— `CLAUDE.md` に `{` が残っていないこと
（残っているとセッション開始時に警告が出ます）。

工房レーン（`/tool` `/note`）だけを使うなら、「コマンド」と
「ディレクトリ構成」の 2 節が埋まっていれば動きます。

## 1. どちらのレーンか（10 秒で決める）

問いは 1 つ。 **作りたいものを 1 行で言い切れるか。**

```
言い切れる    →  工房レーン        /tool
言い切れない  →  反復開発レーン    /backlog init
```

「2 つの CSV の差分を出したい」は言い切れているので工房。
「受領した CSV を検査して不備を報告する仕組みが欲しい」は
入力・検査・出力・設定と複数のことを含むので反復開発。

**迷ったら工房で作る。** 育ったら `/promote` で反復開発レーンへ上げられます。
逆はできません（作りすぎたものを小さくは戻せない）。

---

# ユースケース

## UC-1. 道具が 1 本欲しい（工房・数十分）

```
/tool 2つの CSV の差分を行単位で出したい
```

起きること:

1. 既存ツールと重複していないか `CATALOG.md` を確認（重複なら既存に引数を足す提案）
2. 雛形を生成し、 **テストを先に書いて Red を確認**
3. 実装して Green
4. `README.md` に **実際に動かした出力** を貼り、カタログへ登録してコミット

できあがるもの:

```
workshop/tools/csv-diff/
├─ README.md        取扱説明書（実行例つき）
├─ main.py          実装（1 ファイル完結）
└─ test_main.py     テスト
```

要求仕様書も設計書もバックログも作りません。 **省くのは前工程であって
テストではない** ので、テスト先行だけは工房でも守ります。

次にやること: 使ってみて、踏んだ落とし穴を `README.md` に足す。

## UC-2. 調べたことを残したい（工房・5 分）

```
/note PowerShell 5.1 は BOM 無し UTF-8 の .ps1 を ANSI として読む
```

`workshop/notes/2026-07-30-ps51-encoding.md` に 1 本残り、索引が更新されます。

内容がゴール・仕様・設計に属するものだった場合は、ノートにせず
適切な場所（`docs/concept.md` や `docs/slices/`）へ案内されます。

## UC-3. 前に作ったもの、どこだっけ

```
/workshop search csv
/workshop list
/workshop tidy       … 散らかってきたとき（月 1 回くらい）
```

## UC-4. 腰を据えて作る（反復開発・本命）★

一連の流れを通しで示します。 **順番を飛ばさないのは最初の 3 つだけ** です。

### ステップ 1: ゴールとバックログ

```
/backlog init
```

`docs/concept.md` が無ければ「ゴールは何か」「何ができたら完走か」を聞かれます。
**1 行で答えて構いません**（この時点で全部を決めない）。

できるもの:

- `docs/concept.md` —— ゴールと完走の定義
- `docs/backlog.md` —— **進捗と負債の唯一の正** 。縦切りスライス 3〜8 本が
  すべて `L0 未着手` で並ぶ

スライスが 8 本を超えたらゴールが大きすぎるので、分割を提案されます。

### ステップ 2: 骨組みを作り切る（プロジェクトに 1 回だけ）

```
/skeleton
```

最も薄い経路 1 本を、端から端まで通します。 **ここで動くものが手に入ります。**

- E2E テストを **先に** 書いて Red を確認 → 通す最小実装 → 実際に動かす
- 仮実装・ハードコード・1 ファイルへの集約は **すべて許可**
  （ただし負債表への記録が必須）
- 層を分けなくてよい。エラー処理は落ちるだけでよい

ここが従来との最大の違いです。「動くもの」が最後ではなく **最初** に来ます。

### ステップ 3: 反復（★ これを繰り返す）

```
/iterate
```

引数を省くとバックログの最上段を取ります。`/iterate S03` と指定も可。

**1 回で上げるのは成熟度 1 段だけ。** L0 → L1、または L1 → L2。
目安は半日以内。2 段まとめて上げようとしないことが、この仕組みの肝です。

| | やること | やらないこと |
|---|---|---|
| L0 → L1 | 正常系が通る。E2E 1 本 | 境界値・失敗経路・層分け・文書整備 |
| L1 → L2 | 境界値・失敗経路・仮実装の除去 | 現状設計書・マニュアル |
| L2 → L3 | 構造と文書を整える | —— （他人に渡すときだけ） |

### ステップ 4: 迷ったら現在地を聞く

```
/backlog status     … 成熟度の内訳・負債件数・着手中
/backlog 次         … 次の一手を「1 つだけ」返す
```

`/backlog 次` は決められた表に機械的に当てはめるので、その場の気分で
順序が変わりません。 **迷ったらこれを打てばいい** ようにしてあります。

### ステップ 5: 負債がたまったら返す

```
/refactor           … 返す順序の上位から
/refactor D02       … 番号を指定
```

振る舞いは変えません。テストの期待値を変えたくなったらそれは仕様変更なので、
リファクタリングを止めて確認が入ります。

目安は **反復 2 回分の負債がたまったら 1 回挟む** 。

### いつ完成か

| 状態 | 判定 |
|---|---|
| 全スライスが L2 で、自分だけが使う | **完成** 。次のゴールへ |
| 全スライスが L2 で、他人に渡す | `/refactor` と文書同期で L3 へ |

**L3 は他人に渡すためのレベル** です。自分用の道具を L3 に上げるのは過剰。
ここを勘違いすると、また終わらなくなります。

## UC-5. 途中で「あれも欲しい」と思いついた

```
/backlog CSV を Excel 形式でも読みたい
```

`docs/concept.md` のゴールに寄与するかを判定し、

- 寄与する → スライス表に追加（価値と依存を決める）
- 寄与しない → **「今回のゴールの外」表** に理由つきで降ろす

思いつきを捨てずに、かつ今回のゴールを膨らませない置き場所があります。

## UC-6. 工房ツールが育ってきた

次のどれかに当てはまったら昇格の合図です。

- 他のツールから使いたくなった / 1 ファイルに収まらない /
  自分以外が使う / 壊れると困る / 設定を持ち始めた

```
/promote csv-diff
```

**ゼロからやり直しません。** 動いているツールは `L1 動く` 相当から出発し、
`/iterate` で成熟度を上げていきます。

逆に「一度きりの調査」「壊れても数分で書き直せる」ものは
**昇格させない** のが正解です。全部を反復開発レーンに上げると速度が死にます。

## UC-7. 既存のコードに後から入れる

```
1. /setup-project      … 既存の pyproject.toml 等から言語・コマンドを読み取る
2. /backlog init       … 既存機能を縦切りスライスとして棚卸しする
```

既に動いている機能は最初から `L1 動く` として並べます。骨組みは
通っている扱いなので `/skeleton` は飛ばし、`/iterate` から始めます。

## UC-8. 要求を見たい（USDM）

要求は **HTML の表** で書きます。形は Excel の USDM テンプレートと同じ
（カテゴリ名 / 要求 / 要求ID / 要求仕様）で、ブラウザでそのまま開けます。

| | パス |
|---|---|
| 手書きの正（機能要求） | `docs/usdm/src/S##-<name>.html` |
| 手書きの正（品質要求） | `docs/usdm/src/Q##-<name>.html` |
| 要求一覧（生成物） | `docs/usdm/index.html` |

1 スライスにつき 1 枚。 **その 1 枚をダブルクリックすれば、そのスライスの
要求だけが表で読めます。** 全体を通しで見るには束ねた 1 枚を生成します:

```
<プロファイルのツール実行コマンド> .claude/tools/build_usdm.py
```

`docs/usdm/index.html` が生成されるので、 **ダブルクリックで開くだけ** 。
外部への通信は一切なく、サーバもネットワークも不要です。

**手書きの 1 枚も、束ねた一覧も、操作は同じです**
（見た目と操作は `.claude/skills/usdm/usdm.css` / `usdm.js` の 1 組を共有）:

- 検索（要求・理由・仕様・評価尺度）
- 仕様の絞り込み（すべて / 未検証のみ / 検証済みのみ）
- 文書 / 要求グループ / 要求 / 仕様グループの **4 段階の折りたたみ** 。
  「すべて閉じる」で要求だけのアウトラインになります

同じコマンドが記法の検証も兼ねます（理由の欠落・番号の不整合は
終了コード 1）。古くなっていないかの確認は `--check`。

記入例（話題沸騰ポット）は `.claude/skills/usdm/example/` にあります。

---

# 1 日の典型的な流れ

```
朝    /backlog status      現在地を見る
      /iterate             反復 1 本（半日以内）
昼    /iterate             もう 1 本
夕    /backlog status      成熟度が上がったことを確認
```

数反復ごとに `/refactor` を 1 回、数十反復ごとに `/improve-process`
（プロセス自体の改善）を挟みます。

# よくある詰まり方

| 症状 | 原因 | 対処 |
|---|---|---|
| エージェントがコマンドやパスを推測し始める | `CLAUDE.md` に `{` が残っている | `/setup-project` |
| いつまでも完璧に作ろうとして終わらない | 目標成熟度を伝えていない | 「L1 まで」と明示する。`/iterate` は 1 段しか上げない |
| 次に何をやるか決められない | —— | `/backlog 次`（1 つだけ返る） |
| 1 反復が半日で終わらない | スライスが大きすぎる | `/backlog` で分割する |
| 仕様が 3 条を超える・要求が 2 個になる | 同上 | 同上 |
| 手抜きが気持ち悪い | —— | 負債表に書けば **正式に許可されます** 。書かずに残すことだけが規約違反 |
| 「これも直したい」が止まらない | 反復とリファクタリングを混ぜている | `/refactor` に回す。1 反復 1 目的 |

# コマンド早見表

**反復開発レーン**

| コマンド | 用途 |
|---|---|
| `/backlog init` | ゴールとスライス一覧を作る |
| `/backlog status` / `/backlog 次` | 現在地を見る / 次の一手を 1 つ決める |
| `/backlog <自由記述>` | スライス・負債を足す |
| `/skeleton` | 骨組みを作り切る（1 回だけ） |
| `/iterate [S##]` | **既定の入口。** 成熟度を 1 段上げる |
| `/refactor [D##]` | 負債を返す（振る舞いは変えない） |
| `/add-feature` | 厚い経路（不可逆・外部公開・安全機構・データ形式のときだけ） |

**工房レーン**

| コマンド | 用途 |
|---|---|
| `/tool <1 行>` | ツールを 1 本作りきる |
| `/note <内容>` | ノートを 1 本残す |
| `/workshop list\|search <語>\|tidy` | 一覧・検索・棚卸し |
| `/promote <tool> [S##]` | 反復開発レーンへ昇格 |

**共通**

| コマンド | 用途 |
|---|---|
| `/setup-project` | プロファイルを確定させる（最初に 1 回） |
| `/check-docs` / `/review-docs` | 文書の機械点検 / 内容レビュー |
| `/improve-process` | プロセス自体を改善する（数反復ごと） |

# もっと詳しく

| 知りたいこと | 読むもの |
|---|---|
| 全体の方針と構成 | `README.md` |
| 完了条件（何をもって L1・L2・L3 か） | `.claude/skills/agile-process/maturity.md` |
| プロセスの考え方 | `.claude/skills/agile-process/SKILL.md` |
| 要求の書き方（USDM） | `.claude/skills/usdm/SKILL.md` |
| 層の分け方 | `.claude/skills/layered-architecture/SKILL.md` |
| 設計思想・エージェント構成 | `.claude/README.md` |
